#!groovy

pipeline {
    agent any

    stages {
        stage('Check Shell') {
            steps {
                script {
                    // Run a simple shell command
                    sh 'echo "Shell is running"'
                }
            }
        }
        
        // Add more stages as needed for your pipeline
    }
}
